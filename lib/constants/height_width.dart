import 'dart:js';

import 'package:flutter/material.dart';

class HeightWidth {
  static var h = MediaQuery.of(context as BuildContext).size.height;
  static var w = MediaQuery.of(context as BuildContext).size.width;
}
